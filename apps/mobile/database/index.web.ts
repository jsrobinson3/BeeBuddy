/**
 * Web stub for WatermelonDB database.
 *
 * On web, the app uses TanStack Query hooks (.legacy.ts) instead of
 * WatermelonDB. This stub satisfies the import so the web bundle doesn't
 * try to load the native SQLiteAdapter.
 */

export const database = null;
