EXPO_PUBLIC_API_URL=http://localhost:8000

# Sentry (optional for local dev)
# EXPO_PUBLIC_SENTRY_DSN=
# Required for EAS builds to upload source maps (set as EAS secrets):
# SENTRY_ORG=
# SENTRY_PROJECT=
# SENTRY_AUTH_TOKEN=

# OAuth — Google Sign-In
# See docs/oauth-setup.md for full instructions.
# All three come from Google Cloud Console > APIs & Credentials > OAuth client IDs.
# EXPO_PUBLIC_GOOGLE_CLIENT_ID is the **Web** client ID (same value as GOOGLE_CLIENT_ID on the backend).
# The iOS and Android client IDs are separate OAuth clients created for each platform.
EXPO_PUBLIC_GOOGLE_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_IOS_CLIENT_ID=
EXPO_PUBLIC_GOOGLE_ANDROID_CLIENT_ID=

# Apple Sign-In — no env vars needed (configured via app.json: usesAppleSignIn + expo-apple-authentication plugin)
