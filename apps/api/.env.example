# Application
DEBUG=true
API_V1_PREFIX=/api/v1

# Database
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/beebuddy
# Pool size per worker — keep total (pool_size + max_overflow) × workers < DB max_connections
DB_POOL_SIZE=5
DB_MAX_OVERFLOW=3

# Redis
REDIS_URL=redis://localhost:6379/0

# S3-compatible storage (MinIO for local dev)
AWS_ACCESS_KEY_ID=minioadmin
AWS_SECRET_ACCESS_KEY=minioadmin
S3_BUCKET=beebuddy-photos
S3_ENDPOINT_URL=http://localhost:9000
S3_PUBLIC_URL=http://localhost:9000

# =============================================================================
# Auth (JWT)
# =============================================================================
SECRET_KEY=local-dev-secret-do-not-use-in-production
COOKIE_SECURE=false
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# OAuth2 Social Login
# See docs/oauth-setup.md for full instructions on obtaining these values.
#
# Google: Create OAuth client IDs at https://console.cloud.google.com/apis/credentials
# GOOGLE_CLIENT_ID is the **Web** client ID — used as the audience for ID token verification.
# GOOGLE_CLIENT_SECRET is not used in the native ID-token flow but included for completeness.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
#
# Apple: Configure at https://developer.apple.com/account/resources
# APPLE_CLIENT_ID is your iOS bundle identifier (com.beebuddyai.app).
# APPLE_TEAM_ID is your 10-character Apple Developer Team ID.
# APPLE_KEY_ID and APPLE_PRIVATE_KEY come from a "Sign in with Apple" key.
# Only APPLE_CLIENT_ID is strictly required for native ID-token verification.
APPLE_CLIENT_ID=com.beebuddyai.app
APPLE_TEAM_ID=
APPLE_KEY_ID=
APPLE_PRIVATE_KEY=
#
# Microsoft (not yet implemented)
# MICROSOFT_CLIENT_ID=
# MICROSOFT_CLIENT_SECRET=

# =============================================================================
# LLM Configuration
# =============================================================================
# Provider: ollama, openai, anthropic, bedrock
LLM_PROVIDER=ollama
LLM_MODEL=llama3.2:3b

# Ollama (local - started via docker compose)
OLLAMA_BASE_URL=http://localhost:11434

# OpenAI (uncomment to use)
# LLM_PROVIDER=openai
# LLM_MODEL=gpt-4o-mini
# OPENAI_API_KEY=sk-...

# Anthropic (uncomment to use)
# LLM_PROVIDER=anthropic
# LLM_MODEL=claude-3-haiku-20240307
# ANTHROPIC_API_KEY=sk-ant-...

# =============================================================================
# Email (SendGrid)
# =============================================================================
EMAIL_SUPPRESS=true
EMAIL_FROM_ADDRESS=noreply@beebuddy.app
EMAIL_FROM_NAME=BeeBuddy
# SENDGRID_API_KEY=SG....

# =============================================================================
# Sentry (optional for local dev)
# =============================================================================
# SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0
# SENTRY_ENVIRONMENT=development
# SENTRY_TRACES_SAMPLE_RATE=0.2

# =============================================================================
# CORS
# =============================================================================
CORS_ORIGINS=["http://localhost:3000","http://localhost:8081","http://localhost:19006"]
